export type PlanAbonnementClient = {
  id: string
  type: string
  prix: string
  createdAt: string
  updatedAt: string
  deleteAt: any
  deletedAt: any
}

export type PlanAbonnementClientQuery = {
    page: number,
    limit: number
}
